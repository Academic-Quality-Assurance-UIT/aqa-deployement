{
  "name": "aqa-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "turbo format",
    "docker:build": "docker compose build",
    "docker:push": "docker compose push"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
